import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        borderRadius: 100,
        backgroundColor: '#6E7377',
        borderWidth: 1,
        borderColor: '#6E7377',
    },
});
export default styles;
